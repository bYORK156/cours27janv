

//// LES OPERATEURS LOGIQUES -------------------------------------

let prenom
let loginAge
let loginPrenom
let Age


// l'operateur ET "&&"

// si je cumule deux conditons
// si prenom === loginPrenom et Age === loginAge

if((prenom === loginPrenom) && (age == loginAge)){
    // si ces 2 conditons sont remplis alors le code s'exécute

}

if ( a && b ){

}

// l'opéarteur OU "||"
if( a || b){
    //ici il faut au moins une de ces deux conditions pour que le code s'execute

}

// l'opérateur " !" signifie le contraire

if (!userIsLogin){ // est différent de true
        // si le user n'est  pas connecté, si userIsLogin est false

}

// ce qui revient  à écrire
if (userIsLogin===false){

    // si le user n'est  pas connecté, si userIsLogin est false 

    }
